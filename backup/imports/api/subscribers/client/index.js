// For now we don't need any client-side methods for subscribers
// There's no need to push a local version of the subscriber db or add latency compensation

// We will probably have methods here for an admin dashboard that shows a list of subscribers